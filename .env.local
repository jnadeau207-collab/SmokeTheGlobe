# ---- Required for Prisma / PostgreSQL ----
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/smoketheglobe"

# ---- Required for NextAuth ----
NEXTAUTH_URL="http://localhost:3000"

# Generate a random string with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET="paste_the_random_hex_from_that_node_command_here"

# ---- Used by the scheduler API ----
CRON_SECRET="some-long-random-string-change-me"

# ---- Algolia (can be dummy in dev) ----
ALGOLIA_APP_ID="dev-placeholder"
ALGOLIA_ADMIN_KEY="dev-placeholder"
ALGOLIA_SEARCH_KEY="dev-placeholder"
ALGOLIA_INDEX_DISPENSARIES="cartfax_dispensaries"
ALGOLIA_INDEX_BATCHES="cartfax_batches"

# ---- Google Maps (can be dummy in dev) ----
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="dev-placeholder"

# ---- AWS S3 (can be dummy in dev) ----
AWS_ACCESS_KEY_ID="dev-placeholder"
AWS_SECRET_ACCESS_KEY="dev-placeholder"
AWS_REGION="us-east-1"
AWS_S3_BUCKET="dev-placeholder"
